@model graduation_project.Models.Booking

@{
    ViewBag.Title = "Details";
}

<h2>الملاحظات</h2>
<div class="col-xl-12 col-sm-6 mb-xl-0 mb-4">
    <div class="card">
        <div class="card-body p-3">
            <div class="row">
                <div class="col-8">
                    <div class="numbers">
                        <h5 class="font-weight-bolder">
                           الملاحظة
                        </h5>
                        <p class="text-sm mb-0 text-uppercase font-weight-bold p-4">@Model.note</p>

                        
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>


    </dl>
</div>
<p class="p-3">
    @Html.ActionLink("تعديل", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("الرجوع لقائمة الحجز", "Index")
</p>
